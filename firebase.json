{
  "storage": {
    "rules": "storage.rules"
  },
  "firestore": [
    {
      "database": "(default)",
      "rules": "firestore.rules"
    },
    {
      "database": "marketing",
      "rules": "firestore.rules"
    }
  ]
}
